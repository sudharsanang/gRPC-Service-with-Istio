apiVersion: apps/v1
kind: Deployment
metadata:
  name: grpc-echo-v1
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: grpc-echo
  template:
    metadata:
      labels:
        app: grpc-echo
        version: v1
    spec:
      containers:
      - name: grpc-echo
        image: {{ .Values.image }}
        imagePullPolicy: Never
        ports:
        - containerPort: 50051
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
